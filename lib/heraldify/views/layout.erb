<!DOCTYPE HTML>
<html lang="en">
    <head>
        <meta charset="utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
                <title><%= !@title.to_s.empty? ? @title.to_s + ' | Heraldify' : 'The no. 1 destination for curated news | Heraldify' %> </title>
                <% settings.assets['styles'].each do |style| %>
                    <link rel="stylesheet" type="text/css" href="<%= "#{settings.assets['folder']}/#{style['filename']}"  %>" />
                <% end %>
    </head>
<body>
	<%= erb :'partials/header' %>
	<%= yield %>
	<%= erb :'partials/footer' %>
</body>
</html>
